<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Score Settler" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>

    <% if current_user %>
        You are signed in as <%= current_user.username %>.
        <nav>
          <ul>
            <li> <%= link_to 'Logout', destroy_user_session_path, :method => :delete %> </li>
            <li><%= link_to 'Groups', user_path(current_user.id), :method => :get %></li>
          </ul>
        </nav>
      <% else %>
        Please <%= link_to 'sign in', new_user_session_path %> first.
     <% end %>

  </head>

  <body>

    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>